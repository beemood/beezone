Input validation library built on top of `class-validator` and `class-transformer` to implement all validaitons operation with a single decorator `@Valiation({ ...options })`.

## Install

```bash
pnpm add @beezone/validation
```
